const NAME = "fakestoreapi",
      DOMAIN = `https://${NAME}.com`,
      SITE = `${DOMAIN}/products`,
      CATEGORIES = `${SITE}/categories`,
      SINGLE_CATEGORY = `${SITE}/category/`


export default {
    NAME,
    DOMAIN,
    SITE,
    CATEGORIES,
    SINGLE_CATEGORY
}
      